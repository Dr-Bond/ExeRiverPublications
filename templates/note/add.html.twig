{% extends 'base.html.twig' %}

{% block title %}Add Note{% endblock %}

{% block body %}
    <h1>Add Note</h1>
    <h2>{{ book }}</h2>
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.noteType) }}
        {{ form_row(form.content) }}
        {{ form_row(form.save) }}
    {{ form_end(form) }}
{% endblock %}